#!/usr/bin/env bash
#
# Install zsh

message="Do you want to (re)install zsh?"

function add_app {
  if [ "$system" == "arch" ]; then
    #TODO: setup arch
    apps="$apps zsh"
  elif [ "$system" == "ubuntu" ]; then
    #TODO: ubuntu setup
    apps="$apps zsh"
  fi
  doing 'Adding zsh'
}
